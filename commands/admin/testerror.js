//const errors = require('../utils/errors.js')

module.exports = {
    name: 'testerror',
    description: 'test pour le erreur',
    execute (message, client, config) {
     //   errors.noPerms(message, "test perm");
    //    console.log(config.luti.id)
      console.log(config.prefix)
    
    }
}